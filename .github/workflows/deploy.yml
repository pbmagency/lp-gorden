name: Deploy to VPS 2 (Staging)

on:
  push:
    branches:
      - main

env:
  SERVER_IP: '72.61.142.238'
  SERVER_USER: 'root'
  TARGET_DIR_LARAVEL: '/home/pbmagency.id/sub2_laravel'
  TARGET_DIR_PUBLIC: '/home/pbmagency.id/sub2'

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'

      - name: Install Dependencies (Composer)
        run: composer install --no-interaction --prefer-dist --optimize-autoloader

      - name: Setup Node.js & Build Assets
        run: |
          npm install
          npm run build

      - name: Setup SSH Key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.DEPLOY_KEY_VPS2 }}

      - name: Add Server to Known Hosts
        run: ssh-keyscan -H ${{ env.SERVER_IP }} >> ~/.ssh/known_hosts

      - name: Deploy Core Laravel Files
        run: |
          rsync -avz --no-perms --no-owner --no-group --delete \
          --exclude='.git' \
          --exclude='/public' \
          --exclude='.env' \
          ./ ${{ env.SERVER_USER }}@${{ env.SERVER_IP }}:${{ env.TARGET_DIR_LARAVEL }}/

      - name: Deploy Public Files
        run: |
          rsync -avz --no-perms --no-owner --no-group --delete \
          ./public/ ${{ env.SERVER_USER }}@${{ env.SERVER_IP }}:${{ env.TARGET_DIR_PUBLIC }}/

      - name: Create .env file on Server
        run: |
          ssh ${{ env.SERVER_USER }}@${{ env.SERVER_IP }} "
          cat << 'EOF' > ${{ env.TARGET_DIR_LARAVEL }}/.env
          APP_NAME=Laravel
          APP_ENV=production
          APP_KEY=base64:rP6NsHlCN2LWiyanp69WXcdb8Xq7TwxIRG2ROPGx3jc=
          APP_DEBUG=true
          APP_URL=https://sub2.pbmagency.id
          APP_LOCALE=en
          APP_FALLBACK_LOCALE=en
          APP_FAKER_LOCALE=en_US
          APP_MAINTENANCE_DRIVER=file
          BCRYPT_ROUNDS=12
          LOG_CHANNEL=stack
          LOG_STACK=single
          LOG_DEPRECATIONS_CHANNEL=null
          LOG_LEVEL=debug
          DB_CONNECTION=mysql
          DB_HOST=127.0.0.1
          DB_PORT=3306
          DB_DATABASE=pbma_sub2
          DB_USERNAME=pbma_sub2
          DB_PASSWORD=fK@Ll9NJl5cpIO4C
          SESSION_DRIVER=database
          SESSION_LIFETIME=120
          SESSION_ENCRYPT=false
          SESSION_PATH=/
          SESSION_DOMAIN=null
          BROADCAST_CONNECTION=log
          FILESYSTEM_DISK=local
          QUEUE_CONNECTION=database
          CACHE_STORE=database
          MEMCACHED_HOST=127.0.0.1
          REDIS_CLIENT=phpredis
          REDIS_HOST=127.0.0.1
          REDIS_PASSWORD=null
          REDIS_PORT=6379
          MAIL_MAILER=log
          MAIL_SCHEME=null
          MAIL_HOST=127.0.0.1
          MAIL_PORT=2525
          MAIL_USERNAME=null
          MAIL_PASSWORD=null
          MAIL_FROM_ADDRESS=\"hello@example.com\"
          MAIL_FROM_NAME=\"\${APP_NAME}\"
          AWS_ACCESS_KEY_ID=
          AWS_SECRET_ACCESS_KEY=
          AWS_DEFAULT_REGION=us-east-1
          AWS_BUCKET=
          AWS_USE_PATH_STYLE_ENDPOINT=false
          VITE_APP_NAME=\"\${APP_NAME}\"
          POSTHOG_PROJECT_TOKEN=
          POSTHOG_HOST=https://us.i.posthog.com
          POSTHOG_DISABLED=false
          POSTHOG_CLIENT_ID=your-client-id
          POSTHOG_CLIENT_TYPE=ctwa_leads
          VITE_POSTHOG_KEY=\"\${POSTHOG_PROJECT_TOKEN}\"
          VITE_POSTHOG_HOST=\"\${POSTHOG_HOST}\"
          VITE_POSTHOG_CLIENT_ID=\"\${POSTHOG_CLIENT_ID}\"
          VITE_POSTHOG_CLIENT_TYPE=\"\${POSTHOG_CLIENT_TYPE}\"
          META_PIXEL_ID=1793418607991367
          META_ACCESS_TOKEN=EAAMy4PevDW8BSDvtnTqF2ThQ9VSkh6Pszq9MxhjuIvbh7e6a96x1YmZAtzslUTqZCPqM59Y9sxC1frhJ3xrZAsnfzIcU8gj8xiStoH1iVF3nVRS1HbGun1rZCNccodJhz2RRZB9nzBUcjeLjtEF7Y6RbsY3pPI1zbq5lfhHxsp9VSk9ZB7a8HYMSO1wWAqLpZAoCAZDZD
          EOF
          "

      - name: Adjust Permissions, Generate Key, Migrate & Clear Cache
        run: |
          ssh ${{ env.SERVER_USER }}@${{ env.SERVER_IP }} "
            cd ${{ env.TARGET_DIR_LARAVEL }}
            chmod -R 775 storage bootstrap/cache
            php artisan key:generate --force
            php artisan migrate --force
            php artisan optimize:clear
          "